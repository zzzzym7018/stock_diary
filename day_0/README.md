# Day0 Investment Journal

站点入口路径：`/yyy_stock_diary/`（代号固定为 `yyy`，与仓库根目录 `vercel.json` 中的路径一致）。

Vercel 部署方式：

1. 把资产截图放到 `day_0/assets/day0-portfolio.png`
2. 在 Vercel 导入本仓库根目录
3. Framework 选择 Other / Static
4. 访问站点根路径 `/` 会跳转到 `/yyy_stock_diary/`
